# Direct dependencies only. Transitive pins are left to pip so a security
# patch in kombu or click does not require a change here.

# core
Django==6.0.7
python-decouple==3.8
Pillow==12.3.0
psycopg[binary]==3.3.4

# filtering
django-filter==26.1

# background work
celery[redis]==5.6.3
django-celery-beat==2.9.0

# router credential encryption (P1)
cryptography==49.0.0
